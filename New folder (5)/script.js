
    /*<script>
        // Function to handle donation form submission
        document.getElementById("donationForm").addEventListener("submit", function(event)) {
            event.preventDefault(); // Prevent form from submitting

            // Get values from the form
            var organization = document.getElementById("organization").value;
            var amount = document.getElementById("amount").value;

            // Hide donation form and show confirmation
            document.getElementById("donation-section").style.display = "none";
            document.getElementById("donation-confirmation").style.display = "block";

            // Display donation details
            var message = "You have donated " + amount + " INR to " + organization + ".";
            document.getElementById("donation-message").innerText = message;
       
    </script>*/
